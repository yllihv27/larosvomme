<h2 style="padding-top;padding-left: 50px;">Opprett by</h2>

<div class="page-card" style="padding: 30px;">

  <%= form_with(model: city, local: true) do |form| %>
    <% if city.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

        <ul>
        <% city.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :by_navn %>
      <%= form.text_field :name %>
    </div><br>

    <div class="actions">
      <%= form.submit 'Opprett by', class: 'blue-button' %>
    </div>
  <% end %>
</div>
